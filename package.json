{
  "name": "otherlite",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.32.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.9.14"
  },
  "engines": {
    "node": ">=20"
  }
}
